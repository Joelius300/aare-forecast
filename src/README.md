- `aare-*` packages are libraries with import roots `aare_*` and identical package names (`aare-*`).
- `oraku-*` packages are services/applications with import roots `oraku_*` but longer package names (`aare-oraku-*`)
  corresponding to the docker image and dokku app names.
