[project]
name = "aare-plotly"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "narwhals>2.0.0",
    "plotly>=6.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
