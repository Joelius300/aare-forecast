[project]
name = "aare-timescale"
version = "0.1.0"
description = "TimescaleDB functionality for aare-oraku"
requires-python = ">=3.12"
dependencies = [
    "psycopg[binary,pool]>=3.2.9",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
