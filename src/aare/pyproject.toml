[project]
name = "aare"
version = "0.1.0"
description = "Core code in lightweight package for all use cases (train, inference, api, etc.)"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
